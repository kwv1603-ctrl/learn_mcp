# AntiGravity 金融分析 Agent 标准操作程序（SOP）

> **版本**: V2.0 | **生效日期**: 2026-07-22

## 0. 规则边界、引用与优先级

本项目的报告生成契约由三份文件共同组成，任何一份都不得脱离另外两份单独解释：

1. `rules.md`：定义工作流、数据纪律、模型适用性和不可触碰的红线。
2. `rating_criteria.md`：定义评分、数据覆盖率、硬性排除和最终评级映射，是评级的唯一判定依据。
3. `report_template.md`：定义最终报告的章节、表格和必须披露的审计字段。

发生冲突时按职责处理：工作流与数据纪律以 `rules.md` 为准；评级以 `rating_criteria.md` 为准；报告结构以 `report_template.md` 为准；工具默认值不得覆盖上述三份文件。发现无法消解的冲突时必须停止评级并标记“暂不评级”，不得自行选择有利口径。

## 1. 核心工作流（The Workflow）

1. **预检**：收到股票代码后，第一步必须调用 `run_report_readiness_check`。预检必须分别检查年度财务、季度财务、估值、技术面、同行、市场归属、评分完整性和待补外部证据。
2. **数据获取（效率优先）**：严格遵守“本地数据库 > 自动 API > 官方披露/监管文件 > 其他可靠公开来源”的优先级。禁止在尚未完成本地与自动 API 预检时直接搜索年报原件。
3. **重大事件预检（Catalyst Scan）**：必须通过外部搜索核实近 6 个月内是否存在分拆上市、并购重组、重大资产出售、核心业务独立融资、重大监管处罚或其他可能改变报表可比性的事件，并记录搜索日期、来源和结论。
4. **补齐强制输入**：根据预检结果补齐最新季度、当前年度已公布季度序列、同行、核心 KPI、同币种无风险利率、重大事件和分析师共识。无法补齐时按第 2 节的缺失数据规则处理。
5. **撰写报告**：严格按照 `report_template.md` 生成 Markdown 报告，保存到 `./reports/`，命名为 `symbol_analysis_YYYYMMDD.md`。`symbol` 必须使用原始行情代码，只允许字母、数字、点、连字符和脱字符。所有章节和规定行必须保留；不适用项填写 `N/A（原因）`，不得删除。
6. **强制校验（Veto Gate）**：生成后必须调用 `validate_report`。状态为 `fail` 时必须修复并重新校验；核心项目只有 warning 但无法核实时，也不得宣称“完全通过”。

## 2. 数据真实性与缺失数据纪律

### 2.1 数据类型

报告中的数值必须明确属于以下四类之一：

- **披露值**：来自公司、监管机构、交易所或结构化财务数据源的原始值。
- **可复算衍生值**：全部输入有来源、公式明确且可复算的指标，例如利润率、CAGR、每股值。
- **估算值/情景假设**：用于前瞻估值的假设值，必须标注“估算/假设”、依据和敏感性，不得伪装成历史事实。
- **缺失值**：填 `N/A（缺失原因）`，不得以行业比例、相邻年度或主观判断补成历史事实。

“零捏造”不禁止使用透明公式计算衍生指标，但禁止把缺失的原始输入反推为确定事实。

### 2.2 评分覆盖率

缺失输入不得人工补分。所有评分必须同时披露 `score_coverage`，具体限制以 `rating_criteria.md` 为准。若关键估值、最新季度或主营业务证据不足以支持结论，必须输出“暂不评级”，不能为了完成模板强行给出六级投资评级。

### 2.3 口径一致性

同一公式的收入、利润、现金流、股本和币种必须来自同一期间、同一合并/归母口径，并显式说明。跨币种估值必须披露汇率日期；折现率或无风险利率原则上应与估值币种匹配，使用替代利率时必须说明基差风险。

## 3. 主人盈余与资本开支纪律

1. Owner Earnings 使用 `净利润 + D&A - 维护性 CapEx`。`总 CapEx`、`固定资产 CapEx`、`无形资产投入`、`维护性 CapEx` 和 `增长性 CapEx` 是不同概念，不得混用。
2. API 未直接披露维护性 CapEx 时，允许给出有依据的区间估算，但必须标注方法和敏感性；启发式估算不得作为披露值，也不得在输入缺失时获得完整评分。
3. 人工传入的 `override_maintenance_capex` 代表已经核验的维护性 CapEx，工具不得再次套用比例或 D&A 下限。
4. **A 股专项检查**：代码以 `.SS`、`.SZ` 或 `.SH` 结尾时，必须检查财务报表附注，先拆分固定资产与无形资产相关现金支出，再独立判断维护性与增长性投入。仅完成“固定资产/无形资产”拆分不能视为完成维护性 CapEx 估计。
5. 矿业、晶圆代工等重资产企业可以在第 6.1 章展示原始与调整后 Owner Earnings，在第 6.3 章说明评分影响；调整必须有产能维持、折旧、公司 sustaining CapEx 指引或其他可核验证据。

## 4. 季报与时间新鲜度

1. 报告必须包含最新已公布季度与去年同期对比。
2. 同时必须汇总当前会计年度所有已公布季度的序列或累计趋势；不要求把每个季度都扩展为完整年度表，但必须能识别年度内边际变化。
3. 当前年度尚无季报时，使用最近一期经审计年报，并在评分覆盖率和结论中明确降级。
4. 报告必须记录“数据截至日期”，不得以未来公告安排或市场预期冒充已披露数据。

## 5. 增长率与估值纪律

1. 前瞻增长率不得使用单季爆发增速或低基数反弹直接外推。
2. `g_graham` 原则上使用剔除异常项后的长期每股盈利增长率；若仅能取得核心 EBIT CAGR，必须说明从 EBIT 到 EPS 的税率、利息、少数股东和股本变化风险。
3. `g_dcf` 使用正常化 Owner Earnings/FCF 的长期增长率；`g_scenario` 使用明确预测期的 EPS 或每股现金流增长率。三个增长率不得混为同一字段。
4. 所有长期前瞻增长率默认上限为 15%；科技/高成长公司不得以短期超高速增长突破该上限。更低的行业成熟度上限应优先使用。
5. Graham 模型不是所有行业的强制主模型。银行、保险、资源周期股、负 EPS 公司及业务结构发生重大变化的公司，应使用 `rating_criteria.md` 认可的行业主模型；Graham 可填 N/A 或仅作为辅助敏感性。
6. 每个情景目标价必须写出预测期、每股指标、估值倍数及倍数来源。只有 `EPS × PE` 算式、没有 PE 的历史/同行/增长依据，仍视为主观估值。
7. 情景概率必须使用 `report_template.md` 的有界公式，三种概率均不得为负且合计必须为 100%。

## 6. 技术面择时纪律

技术面评级约束只以 `run_finagent_reflection` 输出的结构化字段为准：

- `rating_constraint = hard_cap_hold_or_watch`：触发评级上限；具体映射按 `rating_criteria.md`。
- `rating_constraint = timing_caution_only`：不降低投资评级，只影响入场、仓位和风险提示。
- `rating_constraint = none`：无技术面硬约束。

自然语言中出现“熊市风险”“bearish 情景”等词不能自动触发降级。报告第 10 章必须列出 `primary_trend`、`ADX`、`trend_strength` 与 `rating_constraint`。技术工具数据缺失时不得主观填写 `none`，应标记 `unknown`。技术面不属于 100 分综合评分，因此 `unknown` 不改写数值覆盖率，但报告不得宣称已获得技术确认，操作计划只能给出保守的非技术性风险边界。

## 7. 深度分析与人工纠偏

- **警惕价值陷阱**：低估值不等于低风险。基本面持续恶化或存在系统性地缘/政策风险时，必须在第 2、9、11、12 章保持一致披露。
- **重资产纠偏**：高维护性投入既可能是护城河投入，也可能是低资本效率。不得仅凭“垄断地位”自动提高 Owner Earnings 分；纠偏证据和评分影响写入第 6.3 章。
- **SOTP 与备考重估**：发生分拆或重大资产重组时，第 6.1 章重算备考现金流，第 6.2/9.4/9.5 章使用 SOTP 或 Pro-Forma 主模型，第 8.2 章同步更新情景目标价。
- **宏观风险避免重复计分**：第 9.1 章的宏观折价值仅作为压力情景，不再重复进入综合评分；综合评分中的宏观维度已经承担该影响。

## 8. 输出文件纪律

股票分析流程只允许在 `reports/` 中生成最终 Markdown 报告。不得在项目目录留下临时 JSON、CSV、脚本或下载文件。工具确需临时文件时只能使用系统临时目录并自动清理；项目源代码开发、测试和规则维护不受本条限制。
