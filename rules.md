# AntiGravity 金融分析 Agent 标准操作程序 (SOP)

## 1. 核心工作流 (The Workflow)
1. **预检**: 收到股票代码后，第一步**必须**调用 `run_report_readiness_check`。
2. **数据获取 (效率优先)**: 严格遵守：**本地数据库 > 自动 API > 手工搜索** 的优先级。**严禁**在第一步就去网上搜索年报原件。
3. **重大事件预检 (Catalyst Scan)**: 在依赖本地财务数据库之外，**必须**使用网络搜索工具核实标的近 6 个月内是否有“分拆上市 (Spin-off)、并购重组 (M&A) 或核心业务独立融资”等能够颠覆报表结构的重大前瞻性事件。
4. **撰写报告**: 严格按照 `report_template.md` 的结构生成 Markdown 报告，存入 `./reports/` 目录下（命名：`symbol_analysis_YYYYMMDD.md`）。**严禁省略任何一章或表格中的任何一行。**
5. **强制校验 (Veto Gate)**: 报告生成后，**必须**调用 `validate_report` 工具进行结构和逻辑校验。如果校验失败（Status: fail），**必须**根据报错细节修复报告，直到校验通过。

## 2. 绝对不可触碰的高压线 (Zero Tolerance Rules)
- **禁止多余文件 (NO EXTRA FILES)**: 整个分析流程中，**只允许**生成最终的 Markdown 报告文件到 `reports/` 目录下。绝对禁止编写或生成任何多余的数据文件（如 .json、.csv）、测试脚本（.py）或其他临时文件。所有工具调用和数据处理必须在内存中完成。
- **零捏造 (ZERO HALLUCINATION)**: 如果自动化工具和搜索都找不到某个财务数据，**必须填入 N/A 或“该项缺失”**，绝对不允许用比例倒推或编造。
- **A 股 CapEx 陷阱**: 如果标的是 A 股（代码以 .SS / .SZ / .SH 结尾），由于 API 可能将无形资产合并入固定资产，**你必须人工调用搜索工具，查找该公司该年度的《财务报表附注》**，手动拆分 CapEx 并重新计算 Owner Earnings。
- **技术面择时纪律**: 如果在第 10 章（技术面）明确判定为“熊市”或“下降通道”，第 1 章和第 13 章的**评级上限锁定为“观望”或“持有”**，严禁建议直接买入。但如果仅仅是上升趋势中的“超买（Overbought）”或“短期均值回归”，**禁止将其误判为熊市并强制降级**。对于基本面低估且仅是短期超买的标的，应维持“买入”评级，仅在操作建议中提示“耐心等待均线回踩，逢低买入”。
- **季报强制分析 (QUARTERLY ANALYSIS)**: 报告必须包含当前会计年度已公布的最新季度数据（如 2026Q1）。分析时必须对比去年同期（YoY），严禁只使用年报数据进行“静态”分析。如果当前处于 Q1 且暂无当季报表，允许网络搜索最新财报，若无最新年度财报则以最近一期经审计年报为准，但必须在报告中明确说明。
- **一致性**: 报告内所有的财务计算（如计算 Owner Earnings），必须统一使用同一口径（合并口径或归母口径），并在公式中显式标明。
- **严禁估值与概率“拍脑袋” (NO ARBITRARY VALUATION)**: 在进行第 8.2 章的情景推演时，目标价必须由明确的数学算式得出（如 `预测 EPS × 远期 PE乘数`），绝对禁止凭空捏造整数目标价。各种情景的概率分配必须基于前文的量化打分进行数学折算（如：乐观概率锚定宏观总分上限，悲观概率反向锚定巴菲特商业品质），并在报告中显式写明“概率分配依据”，彻底杜绝主观臆测。
- **前瞻增长率测算纪律 (EBIT CAGR & GROWTH CEILING)**: 在测算内在价值（如 Graham 修正公式与 DCF 模型）中的前瞻盈利增长率 $g$ 时，**必须使用“剔除短期大幅波动的长周期核心 EBIT（营业利润）复合年化增长率（CAGR）”作为测算底座**，以规避非经常性损益和低基数反弹的扭曲。对于科技/高成长巨头，**其前瞻增长率 $g$ 的上限必须强制锁定在 15%（安全边际截断防线）**，严禁盲目套用短线爆发性超高增速，确保估值模型具备极高的稳健性。

## 3. 深度分析与人工纠偏 (Deep Reflection)
- **警惕价值陷阱**: 低市盈率可能是因为行业衰退或宏观崩塌。如果发现基本面持续恶化或面临系统性地缘风险，必须在第 2 章和第 9 章进行“折价处理”或标记风险。
- **重资产公司的机器打分失真**: 如果是矿业、晶圆代工等重资产公司，机器给出的 Owner Earnings 可能会因为极高的维护性开支而拿低分。你必须从其绝对定价权和垄断地位出发，在第 6.1 章进行“人工纠偏”。
- **SOTP 与备考重估 (Pro-Forma Valuation)**: 当检测到标的即将剥离重资产或进行重大资产重组时，机器基于 TTM 计算的 CapEx 和主人盈余（Owner Earnings）将严重失真。此时**严禁**死板套用机器给出的负安全边际。你必须在第 6.2 章节引入“备考重估（Pro-Forma）”逻辑，扣除即将剥离的开支，并结合 SOTP（分部加总法）重新核算内在价值与评级。
